# 产品讨论记录 01：切入口选择与 MVP 收敛

> 日期：2026-09-10
> 形式：产品探讨（AI 助手建议，**非最终决策**）
> 输入材料：
> - 《产品构想-以GitHub为桥梁的招聘系统.md》（讨论稿，含 2026-09-10 三项"全纳入"决策）
> - `job-agent-landing` 落地页（Astro：Hero / Problem / Solution / Moat / Demo / Roadmap）
> 状态：**含 4 项待拍板问题，用户尚未最终决定**；本文中"建议"均为助手观点，不代表已决策。

---

## 0. 讨论起点

定位层（一句话定位、双向闭环、真实性护城河、三大方向全纳入）在构想文档与落地页中已经成型且基本自洽。本次讨论要解决的是更靠下一层的问题：

- **第一个可构建、可验证的产品切片是什么？**
- **"先做招聘系统（B）"与"从分析 GitHub 仓库切入"之间存在路径张力，如何收敛？**

---

## 1. 核心张力：宣称"先做 B"，但切入口"仓库分析"本质是 C 端能力

"分析某个 GitHub 仓库 / 账号"本身**不分 B/C**，它是一块可被两侧复用的**共享证据内核**。真正需要选的，是第一层产品界面先服务谁。

| 路径 | 第一个动作 | 优势 | 代价 / 风险 |
| --- | --- | --- | --- |
| **B 端楔子** | HR 粘贴任意候选人 GitHub 链接 → 出报告 | 企业更早付费；公开仓库无需候选人注册，可绕过双边冷启动 | 信任门槛高（要敢据此做雇佣决策）；**"未经本人同意对个人打分并用于雇佣"合规风险最大** |
| **C 端楔子** | 开发者 OAuth 授权本人账号 → 看自己画像 + 改进建议 | PLG 自传播、信任门槛低、授权链最干净 | 离收费更远；早期没有岗位可匹配，价值闭环不完整 |

### 助手建议（待确认）

1. **不要先建"招聘系统"，先把分析引擎做成与 B/C 解耦的独立服务**：输入 GitHub 用户名 → 输出一份**版本化的能力画像 JSON**。B/C 两侧未来都只是这个内核之上的薄界面。
2. 第一份产品界面以 **"本人授权 → 生成可验证画像 → 可选择分享给企业"（候选人持有的凭证）为主脊**；B 端先只提供"公开数据轻预览"。这样同时获得：合规上的"同意"、C 端传播、以及 B 端未来看到的是候选人背书过的权威报告。
3. **落地页即第一个验证界面**：`#demo` 输入框目前只把用户名写入 `localStorage`（见 `job-agent-landing/src/components/Demo.astro`）。把它接上真实分析引擎、返回一份真实（哪怕精简）的画像，落地页立即从"留资页"变为"验证 + 获客通道"，且无需先做账号体系 / 岗位 / 匹配系统。

---

## 2. 护城河再拆解：真实性是"入场券"，反馈闭环才是数据壁垒

构想文档把护城河定为"真实性验证引擎"。讨论中建议拆成两类，避免混为一谈：

### 2.1 真实性检测 = 必要门槛，但不是终局壁垒

- 它是**猫鼠游戏（军备竞赛）**，攻击者会持续迭代刷法。
- 单一标量分数（如落地页示意中的"真实度 98%"）有两个真实危险：
  - **可被反向工程刷分**；
  - **会误伤**——把真实仓库误判为 AI 生成，对候选人是声誉损害，对平台可能带来歧视 / 法律风险。
- 更稳的呈现方式：**可解释、可复核的证据片段 + 置信区间 + 分级状态**，把判断材料交给人，而非输出黑箱分数。这与落地页已有的"可信、可解释、可复核"表述一致，应落到数据结构层。

### 2.2 真正随时间变厚的壁垒 = 能力推断模型 + 结果标签回流

- 对应构想文档 4.3 的"面试结果回流"闭环。
- **"画像预测的能力是否被真实面试 / 录用结果验证"这类 outcome 标签，竞品无法获得**。
- 结论：**真实性谁都能做（入场券），"经结果校准的能力推断"才是网络效应壁垒**。数据采集与产品叙事应从第一天就为"标签回流"预留接口。

---

## 3. "分析一个仓库"分四层，MVP 只做前两层

分析深度直接决定成本、延迟与 GitHub API 速率压力，建议显式分层：

| 层 | 内容 | 数据获取 | 成本 / 风险 |
| --- | --- | --- | --- |
| **L0 元数据 / API** | 语言构成、star/fork、贡献日历、PR/Issue 计数 | 纯 API，不 clone | 快、便宜 |
| **L1 行为 / 时序** | commit 节奏与突发模式、author 一致性、PR 评审周期、Issue→PR 关联、长线持续度 | 纯 API，不 clone | 大部分"造假"在这一层即可暴露 |
| **L2 代码 / 静态** | 架构、复杂度、测试覆盖、重复率、依赖卫生 | 需 **clone** | 资源成本高；**恶意 / 超大仓库带来供应链安全面，必须隔离沙箱** |
| **L3 语义 / AI** | 读代码推断领域与设计决策、生成"你为什么这样设计"类问题 | 调用模型 | 成本 / 延迟最高 |
| **L4 跨库 / 身份** | 跨仓库聚合、被他人项目 merge 的外部贡献（最强信号之一）、多年一致性 | 账号级聚合 | 调用量大 |

### 助手建议（待确认）

- **MVP 只做 L0 + L1（纯 API、不 clone）**，即可产出有区分度的报告；L2/L3/L4 渐进叠加。
- 这同时绕开早期 clone 任意公开仓库的**成本**与**安全**两类问题。

---

## 4. MVP 边界与关键验证动作

### 4.1 做 / 不做

- **做**：输入用户名 →（L0+L1 为主）带证据片段的能力画像 + 真实性分级标记 + 3~5 个基于真实项目的面试问题；结果可缓存、可生成分享链接。
- **先不做**：岗位发布、匹配引擎、投递流程、支付、C 端复杂后台、Gitee 接入——这些属于"系统"，等单份报告被证明有价值后再建。

### 4.2 最关键的去风险动作（先于系统建设）

- 手工挑选 **20~50 个真实 GitHub 账号**，刻意混入两类：
  - 已知的 AI 模板 / 一键生成 / 疑似刷量仓库；
  - 公认的强工程师账号。
- 用分析引擎跑，验证两件事：
  1. **可分性**：能否把两类稳定区分（人工复核真实性信号的精确率 / 召回）；
  2. **可行动性**：真实招聘方是否认为报告"能据此采取下一步动作"。
- **该实验不通过，不启动后续系统建设。**（样本量较小，结论仅用于内部去风险，不外宣。）

### 4.3 现在就要定的抽象：证据源无关的数据契约

- 画像 schema 从第一天就设计为**证据源无关**：统一的 `EvidenceItem { source, claim, evidence_ref, confidence }` 结构。
- 让 GitHub 只是第一个 `EvidenceSource` 插件；未来 Gitee、作品集、项目案例、证书（服务非技术岗）以同构方式接入。
- 构想文档中的"统一能力画像、多元证据源"应在**数据模型层**落地，后补成本高。字段级设计见 `PRD.md` 第 8 章。

---

## 5. 风险与"待查证"项（不拍脑袋给死数）

以下为方向性判断，**具体数字 / 法条在正式技术方案前须核对官方来源**，本次讨论不固化：

| # | 事项 | 当前状态 | 正式落地前要做的事 |
| --- | --- | --- | --- |
| R1 | GitHub API 限额（REST 按次、GraphQL 按"点"计；认证前后差异大） | 仅知"未认证额度很低、认证后大幅提升"，**单次深度分析一个账号需数十至上百次调用** | 以 GitHub 官方最新文档为准；实测"单画像调用预算"，设计缓存 / 条件请求 / token 池 |
| R2 | "分析任意公开账号并用于雇佣决策"的合规 | 方向上涉及画像 / 自动化决策相关规则（GDPR 相关条款、中国《个人信息保护法》），**C 端本人授权路径明显更干净** | 查法规原文 / 法务，明确公开数据使用边界、自动化决策告知、异议与申诉机制 |
| R3 | clone 任意公开仓库的安全与成本 | MVP 不 clone 以规避；L2 上线时必须处理 | 隔离沙箱、仓库大小 / 超时上限、绝不执行仓库内代码 |
| R4 | 市场接受度（HR 愿付费、开发者愿授权） | 当前均为**假设** | 由 4.2 的 20~50 账号实验与落地页真实漏斗验证，不因"已有竞品"就推定需求成立 |

---

## 6. 待用户拍板的 4 个问题

> 以下问题用户**尚未决定**，是 PRD 从草稿转定稿、以及启动开发的前置条件。

1. **第一界面**：以 C 端本人授权为主，还是 B 端粘贴即分析为主？（助手倾向：C 端授权为主脊 + B 端轻预览）
2. **真实性结论形态**：是否接受放弃"真实度 98%"这类单一标量分，改用"证据片段 + 置信区间 + 分级"？
3. **MVP 深度**：是否同意只做 L0+L1、不 clone，并先把落地页 `#demo` 接入真实引擎？
4. **首个验证市场**：是否先押海外 GitHub 生态，Gitee 明确后置？

---

## 7. 下一步

- 产出 `PRD.md`（本次同步完成）：以"共享分析内核 + 落地页真实演示"为不依赖上述抉择的 P0，把受抉择影响的部分标注为"待决策 #n"。
- 待 4 个问题拍板后：补 MVP 技术方案、能力画像 Schema 定稿、分析管道（采集→行为分析→画像生成→报告）设计，并启动 4.2 的标注样本实验。

---

### 信心与证据说明

- 本文档的**架构分层、MVP 收敛、共享内核抽象、护城河拆解**为产品 / 工程判断，助手信心 **8/10**。
- 信心 **低于 7**、已在正文单独标注的项：R1（API 具体配额）、R2（具体法律条款）、R4（市场接受度）。这三项须以官方文档 / 法规原文 / 实验结果为准，文档未做无依据假设。
- 本文未新增外部数据；引用的既有竞品资料沿用《产品构想》文档第 8 章，未独立复核。
